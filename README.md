## ular-maldives-data

Distribution-only repository for the `ular-maldives` app.

Contains the published inventory artifact (`inventory.bin`, AES-256-GCM encrypted) and `manifest.json` for runtime download. Source data, per-resort guides, and build scripts live in the main app repository.
